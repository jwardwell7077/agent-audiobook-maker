# Stub package for langflow (minimal interfaces used by custom components)
"""Typings package providing minimal stubs for ``langflow``.

Only the attributes actually referenced in the codebase are modeled to
keep the stub surface minimal while satisfying type checkers & linters.
"""
