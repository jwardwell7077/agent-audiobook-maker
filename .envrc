if [ -d .venv ]; then
  source .venv/bin/activate
fi

# Strict PATH allowlist (enforced): project shims, venv, core system bins only
ALLOWED_PATHS="$(pwd)/scripts/dev-bin:$(pwd)/.venv/bin:/usr/bin:/bin:/usr/local/bin"
export PATH="$ALLOWED_PATHS"
